[project]
name = "fastapi-learning"
version = "0.1.0"
description = "A Python project with basic APIs built with FastAPI. Intent is to learn FastAPI for API development with Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
]
